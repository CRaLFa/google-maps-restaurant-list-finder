# Python 生成物
__pycache__/
*.pyc
.venv/

# Go 生成物 (`go build ./...` がリポジトリ直下に吐く)
/server

# ローカルの環境変数 (API キーを含む)。雛形は .env.example
.env

# Claude Code のローカル設定 (端末 IP 等を含むためコミットしない)
.claude/settings.local.json

# 一時・バックアップファイル
*.before-*.tsv
test-*.tsv
