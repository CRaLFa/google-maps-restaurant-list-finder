# 今後の実装案: 共有 URL の取得 / リストの削除

[`collect-saved-lists.md`](./collect-saved-lists.md) で確立した adb + uiautomator の手順を踏まえ、
次に実現したい 2 つの操作の実現案を記録する。

いずれも起点は「保存したリスト」の各項目の右端にあるオーバーフローボタンで、
dump 上では `content-desc="<リスト名> に関するオプション"` として現れる
(実機 1080x2340 では x 座標が `[940,*][1060,*]`)。

## オプションメニューの内容 (実機で確認済み)

オーバーフローボタンをタップすると以下の 6 項目が出る。
bounds は開いた位置によって変わるため、毎回 dump して text から引くこと。

| 項目 | 用途 |
| --- | --- |
| 地図に表示しない | — |
| **リストを共有** | 共有 URL の取得に使う |
| **リストを削除** | 保存済みからの削除に使う |
| フィードバックを送信 | — |
| 法的な問題を報告 | — |
| ポリシーの問題を報告 | — |

## 1. 各リストの共有 URL の取得

### クリップボード読み取りには adb-clip を使う

Android 10 以降はフォアグラウンドアプリ以外のクリップボード読み取りが禁止されているため、
`adb shell service call clipboard ...` は使えない。
クリップボードの読み書きには [`polygraphene/adb-clip`](https://github.com/polygraphene/adb-clip) を使う。
`app_process` で shell UID として実行することでこの制限を回避する。**Android 10-16 対応、アプリ導入不要。**

比較検討した [`PRosenb/AdbClipboard`](https://github.com/PRosenb/AdbClipboard) は
アプリ導入 + overlay 権限 + 読み取りごとにフローティングウィンドウのタップが必要で、
自動化には不向きだったため不採用。

#### セットアップ (一度だけ)

`clip` / `clip.jar` は [`tools/adb-clip/`](../tools/adb-clip/) に vendor 済み
(v0.0.3。更新は adb-clip の releases から差し替える)。

```bash
DEV=100.64.1.35:42931   # ワイヤレスデバッグはポートが毎回変わる。都度確認する
adb.exe -s $DEV push tools/adb-clip/clip tools/adb-clip/clip.jar /data/local/tmp/
adb.exe -s $DEV shell chmod 755 /data/local/tmp/clip
```

```bash
# 読み取り (stdout に出る)
adb.exe -s $DEV shell /data/local/tmp/clip
# 書き込み
adb.exe -s $DEV shell "/data/local/tmp/clip 'text'"
```

実機 (Xperia XQ-GE44 / Android 16) で往復動作を確認済み。
**制約は「画面オン + ロック解除」だけ** (収集中はどうせ画面を点けっぱなしにするので問題ない)。

### 実証結果

実機で 1 件 (`平塚市: トップリスト`) を通しで実行し、
`https://maps.app.goo.gl/XXXXXXXXXXXXXXXX?g_st=ac` 形式の短縮 URL の取得に成功した
(この検証時は後述の旧 dump 方式)。

### 手順 (adb-clip 方式)

1. 対象リストのオーバーフローボタンをタップする。
2. メニューの「リストを共有」をタップする。
3. Android の共有シートが開く。左下の「クリップボードにコピー」をタップする。
4. `adb shell /data/local/tmp/clip` でクリップボードを読み、`maps.app.goo.gl` の URL を抜く。
5. 戻るキー (`keyevent 4`) で共有シートを閉じ、次のリストへ進む。

```bash
DEV=100.64.1.35:42931
# コピーをタップした後のクリップボード読み出し部分 (手順 4)
adb.exe -s $DEV shell /data/local/tmp/clip | grep -o 'https://maps\.app\.goo\.gl/[A-Za-z0-9]*' | head -1
```

前のリストの URL を誤って読む事故 (クリップボード未更新) を防ぐため、
コピー前に番兵文字列を `clip 'SENTINEL'` で書き込んでおき、
読み取り結果が番兵のままなら失敗として扱うとよい。

### 注意点

- **自分のリスト (非公開) は共有 URL を持たない。**
  「リストを共有」を押すと共有を有効化する同意ダイアログが先に出る。
  収集対象のフォロー中リストは最初から公開なので即コピーに進める。
  (自分のリストは収集対象外。[`saved-lists.txt`](../saved-lists.txt) はフォロー中リストのみ。)
- **共有シートには端末の連絡先候補 (氏名・メールアドレス・電話番号) が表示される。**
  adb-clip 方式なら URL は `clip` の stdout から取れるため、
  **この画面の UI dump を取る必要が一切ない** (旧 dind 方式の最大の利点)。
  もし dump を取る場合もリポジトリやログに残さないこと。
- **Maps の検索ボックスはスクリプトからの選択コピー (`keycombination`/`KEYCODE_COPY`) に反応しない。**
  クリップボードへ入れるのは共有シートの「クリップボードにコピー」に任せること。
- **1 件あたり数ステップ + 待機で、所要時間は 10 秒程度。**
  385 件を通すと 1 時間前後かかる。バックグラウンド実行前提で組むこと。
- リストのタイトルと URL を対応付けるには、
  オーバーフローボタンの `content-desc` からリスト名を取れる
  (`"<リスト名> に関するオプション"` の接頭辞)。これをキーにして CSV/JSON に落とすとよい。

### 実装スクリプト

[`fetch_share_urls.py`](../fetch_share_urls.py) が上記手順を全件自動化する。
adb-clip 方式で実装済み。リポジトリルートから実行する。

```bash
cd <リポジトリルート>
# 事前に adb-clip を push しておくこと (tools/adb-clip/README.md 参照)
DEV=100.64.1.35:42931 python3 fetch_share_urls.py       # 全件
DEV=... MAX=5 OUT=test.tsv python3 fetch_share_urls.py   # 動作確認 (5 件だけ別ファイルへ)
```

- 結果は `share-urls.tsv` (リスト名 \t URL) に逐次追記。中断しても `done` を読んで再開する。
- コピー前に番兵をクリップボードへ書き、更新されなければ失敗として 2 回までリトライ。
- **「クリップボードにコピー」タップで共有シートは自動で閉じる。**
  余計な戻るキーを押すとリスト画面まで畳んでしまい毎回 top から再構築する羽目になるので、
  シートが残っているときだけ閉じるようにしてある。
- 実機で先頭数件の取得を確認済み (2026-07-25)。1 件あたり 20〜25 秒、385 件で 2〜2.5 時間。

### 旧方式 (dump から貼り付け読み取り) — 非推奨

adb-clip 導入前は、検索ボックスに `keyevent 279` (KEYCODE_PASTE) で貼り付けて
UI dump から URL を抜いていた。PII 混入の dump が必要で手順も長いため、adb-clip 方式に置き換えた。

## 2. 各リストの保存済みからの削除 (実施済み)

2026-07-26 に「トップリスト以外 (トレンド / 地元で人気)」のフォロー中リストを全削除した。
実装は [`delete_lists.py`](../delete_lists.py)。以下は実機 (Xperia XQ-GE44 / Android 16) で確認した挙動。

### 実証した挙動

- **フォロー中リストの「リストを削除」は確認ダイアログが出ない。**
  タップした瞬間に「フォローを解除しています...」と表示され、即フォロー解除される (= 削除)。
- **Undo (スナックバー) は出ない。** 取り消し手段はない。復旧は共有 URL からの再フォローのみ。
- 削除直後、リストは一時的に 0 件表示になり、数秒後に再表示されて **先頭にスクロールが戻る**。
  この 0 件の瞬間を「消えた」と誤判定しないこと (再表示されてから対象の消失を確認する)。
- 「〇〇市: トップリスト」等の自動生成リストが削除後に再サジェストされる現象は、
  今回の範囲では確認されなかった (トップリストは削除していないため未検証のまま)。

### delete_lists.py の方針

- `share-urls.tsv` を既知リストとして読み、**トップリストは残し**、
  それ以外で記録済みのものを削除する。
- **記録に無いリストは削除せず、共有 URL を収集して `share-urls.tsv` に追記**する
  (未記録のものを消さないための安全網)。
- 削除で消えたリストは端末から消えるので、再実行すれば自然に続きから再開できる (resume-safe)。

### ハマりどころ (重要)

- **`am start` による画面再構築を削除ごとに連打すると Maps が ANR (応答なし) を起こす。**
  当初 delete_one が上記「一時的 0 件」を誤判定して即 return し、
  main が 0 件を見て `ensure_list_screen` (am start) を連打 → ANR で停止した。
  対策として delete_one はリスト再表示 + 対象消失を待ち、
  main は 0 件でも数秒待って再 dump し、am start を最後の手段にした。
- **削除を進めるとトップリスト (保持) がリスト上部に溜まり、残りの削除対象が最下部に沈む。**
  削除ごとに先頭へ戻る仕様と相まって「毎回上部のトップリストを大量スクロールで越える」形になり遅くなる。
  それでも 1 パスで最下部まで到達して削除できる。ANR で途中終了しても再実行で続く。
- 長時間の無人実行に備え、**端末のスリープを抑止**しておくとよい
  (`adb shell svc power stayon true` と `settings put system screen_off_timeout` の延長。作業後は元に戻す)。
- 途中終了/ANR に備え、force-stop → 再起動 → 再実行を繰り返すスーパーバイザーで回すと堅牢。
  「削除 0 件 かつ 最下部到達」のパスが出たら完了とみなせる。
  ただしループの自己申告は信用せず、**独立スキャンで残削除対象が 0 であることを検証**すること。

### 結果

- 削除対象 (トレンド / 地元で人気) をすべて削除し、独立スキャンで残 0 を確認。
- トップリスト (約 150 件) は保持。共有 URL は全件 `share-urls.tsv` に保存済み。

## 実装順序の提案

1. 共有 URL の取得を先に全件実行し、リスト名と URL の対応表を作る。(実施済み: 461 件)
2. その対応表をバックアップとして、削除を 1 件で検証する。(実施済み)
3. 問題なければ削除を全件に広げる。(実施済み: トップリスト以外を全削除)
